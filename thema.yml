themes:
  dracula: default
  gruvbox: dark light material
  nord: default
  solarized: dark light
  ayu: dark mirage light
alacritty:
  alacritty.yml:
    destination:
      - $XDG_CONFIG_HOME/alacritty/alacritty.yml
      - $XDG_CONFIG_HOME/alacritty.yml
      - $HOME/.config/alacritty/alacritty.yml
      - $HOME/.alacritty.yml
wallpaper:
  arch.svg:
    destination:
      - $HOME/Pictures/wallpaper/arch.svg
    replace: true
  command:
    - feh --bg-fill --conversion-timeout 1 $HOME/Pictures/wallpaper/arch.svg
bspwm:
  bspwmrc:
    destination:
      - $XDG_CONFIG_HOME/bspwm/bspwmrc
      - $HOME/.config/bspwm/bspwmrc
  command:
    - bspc wm -r
rofi:
  config.rasi:
    destination:
      - $XDG_CONFIG_HOME/rofi/config.rasi
      - $XDG_CONFIG_HOME/rofi/config
      - $HOME/.config/rofi/config.rasi
      - $HOME/.config/rofi/config
    comment: /* */
kitty:
  kitty.conf:
    destination:
      - $XDG_CONFIG_HOME/kitty/kitty.conf
      - $XDG_CONFIG_HOME/kitty.conf
      - $HOME/.config/kitty/kitty.conf
      - $HOME/.kitty.conf
waybar:
  waybar.css:
    destination:
      - $HOME/.config/waybar/style.css
      - $HOME/waybar/style.css
    start_of_file: true
    comment: /* */
polybar:
  polybar.ini:
    destination:
      - $HOME/.config/polybar/config
    start_of_file: true
    comment: ;
Xresources:
  Xresources:
    destination:
      - $HOME/.Xresources
    comment: ! 
zathura:
  zathura.conf:
    destination:
      - $XDG_CONFIG_HOME/zathura/zathurarc
      - $XDG_CONFIG_HOME/zathurarc
      - $HOME/.config/zathura/zathurarc
      - $HOME/.zathurarc
homepage:
  style.css:
    destination:
      - $XDG_CONFIG_HOME/home/web/style.css
      - $HOME/.config/home/web/style.css
      - $HOME/.home/web/style.css
      - $XDG_CONFIG_HOME/home/style.css
      - $HOME/.config/home/style.css
      - $HOME/.home/style.css
    start_of_file: true
    comment: /* */

