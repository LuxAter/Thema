bspc config normal_border_color   "{{ hex bg1 }}"
bspc config active_border_color   "{{ hex bg3 }}"
bspc config focused_border_color  "{{ hex blue }}"
bspc config presel_feedback_color "{{ hex bg4 }}"

